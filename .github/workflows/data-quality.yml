name: Data Quality Pipeline

on:
  push:
    branches: [main, develop]
    paths:
      - 'data/**'
      - 'data_quality/**'
      - 'dbt/**'
  pull_request:
    branches: [main, develop]
    paths:
      - 'data/**'
      - 'data_quality/**'
      - 'dbt/**'
  workflow_dispatch:

jobs:
  quality-checks:
    runs-on: ubuntu-latest
    
    steps:
      # 1. Checkout du code
      - name: Checkout repository
        uses: actions/checkout@v4
      
      # 2. Setup Python
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      
      # 3. Cache des dépendances (accélère les exécutions suivantes)
      - name: Cache pip dependencies
        uses: actions/cache@v3
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('**/requirements.txt') }}
      
      # 4. Installation des dépendances
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pytest pandera pyyaml pandas pyarrow matplotlib
      
      # 5. TESTS PYTEST (Complétude, Validité, Cohérence)
      - name: Run pytest tests
        run: |
          cd data_quality
          pytest tests/ -v --tb=short
      
      # 6. VALIDATION DES DATA CONTRACTS
      - name: Validate Data Contracts
        run: |
          cd data_quality
          python -m validators.contract_validator --all
      
      # 7. PIPELINE COMPLET + GÉNÉRATION DU RAPPORT
      - name: Run full pipeline validation
        run: |
          mkdir -p reports
          cd data_quality
          python -m validators.pipeline_validator --report ../reports/pipeline_report.json
      
      # 8. UPLOAD DES RAPPORTS (toujours, même si échec)
      - name: Upload validation reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: validation-reports
          path: |
            reports/pipeline_report.json
            data_quality/pipeline_report.json
      
      # 9. COMMENTAIRE SUR LA PR (si c'est une pull request)
      - name: Comment PR with results
        if: github.event_name == 'pull_request' && always()
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');
            let report = {};
            try {
              report = JSON.parse(fs.readFileSync('reports/pipeline_report.json', 'utf8'));
            } catch(e) {
              report = { overall_status: 'UNKNOWN', pytest: { passed: false } };
            }
            
            const status = report.overall_status === 'PASS' ? '✅ PASSED' : '❌ FAILED';
            const pytestStatus = report.pytest && report.pytest.passed ? '✅' : '❌';
            const contractStatus = report.overall_status === 'PASS' ? '✅' : '❌';
            
            const body = `## Data Quality Pipeline - ${status}
            
            | Check | Status |
            |-------|--------|
            | Pytest Tests | ${pytestStatus} |
            | Data Contracts | ${contractStatus} |
            
            [View detailed reports](https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }})`;
            
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: body
            });